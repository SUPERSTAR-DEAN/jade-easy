# Jade : TD1 - 谈判

## 基础示例：一对一谈判

---

在 [PingPong](https://github.com/EmmanuelADAM/jade/blob/master/pingPong/) 代码的基础上，
卖方和买方智能体围绕一个价格进行谈判。

定义好消息交换，已知卖方主动发起谈判，提出一个价格：

 - 卖方拥有：
   - 一个提出的价格
   - 一个阈值（低于此值则终止谈判）
   - 一个终止谈判前的最大轮数

 - 买方拥有：
   - 一个提出的价格
   - 一个阈值（高于此值则终止谈判）
   - 一个终止谈判前的最大轮数

 - 对买方：
   - 如果轮数超过最大值，回复拒绝；
   - 如果收到的价格高于上限阈值，回复拒绝；
   - 如果收到的价格与自己提出的价格相近，回复确认；
   - 如果价格介于自己提出的价格和阈值之间，买方把初始出价上调 x%。

- 对卖方：
  - 如果轮数超过最大值，回复拒绝；
  - 如果收到的价格低于下限阈值，回复拒绝；
  - 如果收到的价格与自己提出的价格相近，回复确认；
  - 如果价格介于自己提出的价格和阈值之间，卖方把初始出价下调 x%。

看一下已提供的类，然后运行 `Main` 类的 `main` 方法。

---
**问题 1：** 买方是真人，修改 Buyer 窗口让用户能输入并出报价。-1 会终止谈判。

---
**问题 2：** 一个卖方和多个买方。
  - 卖方向每个已知的买方提出一个价格。
  - 当某个买方出价时，卖方把该买方的修改后价格或决定广播给其他买方。
    - 拒绝：只把做出最后报价的那个买方剔除
    - 接受：拍卖停止

<!--
```
@startuml multiNegociation
participant seller
actor buyer1
actor buyer2
seller -> buyer1 : 出价:100
seller -> buyer2 : 出价:100
seller <- buyer1 : 出价:40
seller -> buyer1 : 出价:90, 40
seller -> buyer2 : 出价:90, 40
seller <- buyer1 : 出价:45
seller -> buyer1 : 出价:80, 45
seller -> buyer2 : 出价:80, 45
seller <- buyer2 : 出价:50
seller -[#red]> buyer1 : 拒绝
seller -[#blue]> buyer2 : 接受:50
@enduml
```
-->
<img src="multiNegociation.png" alt="multiNegociation" width="200"/>


---
**问题 3：** 卖方必须是可配置的（出价、最低价、循环次数）。


---

### 给初学者的提示

**关键难点**：一卖方对多买方时，卖方的每次回复要"广播给所有买方"，
但只有**做出最新报价的那个买方**是"活跃"的。

**动手实验**：
1. 用 `MessageTemplate.MatchContentPattern()` 过滤出"当前活跃买方"的消息
2. 思考：如果两个买方同时出价怎么办？
