# JADE 多智能体编程 —— 学习指导

> 面向零基础 / 初学者的循序渐进路线图。配合本仓库的源码、每个模块目录下的 `readme-zh.md`（中文翻译）和代码内的中文注释一起使用。

---

## 一、这个项目是什么

**JADE**（Java Agent DEvelopment Framework）是经典的 Java 多智能体（Multi-Agent）开发框架，遵循 FIPA 标准，由 Tilab 开发。多智能体编程的核心思想是：不写一个"大而全"的程序，而是写很多个**自主的智能体（Agent）**，让它们通过**消息**互相通信、协作、协商，共同完成一个目标。

本仓库（作者 EmmanuelADAM）是 JADE 的一套**成体系的教学代码**，从最简单的"一个智能体打印一句话"，一路讲到消息通信、服务注册、状态机、交互协议（拍卖、投票）、再到综合案例和 LLM 集成。

**一个关键区别**：本仓库用的不是官方 Tilab 的 JADE，而是作者自己维护的升级版 **JadeUPHF.jar**（基于 Java 25，使用**虚拟线程**，单机最多可跑 10000 个智能体）。所以：

> **运行环境要求：JDK 25（或更高）**，而不是通常的 JDK 8/17。

这个库文件就在仓库里：`external-libraries/JadeUPHF.jar`，不需要你另外去网上下载。

---

## 二、环境准备（只做一次）

你已经基本完成了，这里整理成清单供对照：

1. **装 JDK 25**（Oracle / Microsoft / Adoptium 任一下载，Windows x64 的 `.msi`，安装时勾选 "Set JAVA_HOME"）。
2. **确认版本**：命令行运行 `java -version`，应显示 `25.x`。
3. **VS Code 装 "Extension Pack for Java"**（你已装好：Red Hat Java + 调试 + Maven 等）。
4. **把三个 jar 加入 classpath**：本项目没有 Maven/Gradle，需要手动引用 jar。已通过 `.vscode/settings.json` 配置好：
   - `external-libraries/JadeUPHF.jar`（JADE 平台）
   - `external-libraries/json-20250517.jar`（JSON 处理，个别模块用）
   - `external-libraries/opencsv-3.9-3.jar`（CSV 读写，个别模块用）
5. **验证安装**：运行 `helloworldSolo/AgentHello.java` 的 `main`，控制台应打印出消息、JADE 容器启动日志，就算成功了。

> 常见坑：如果报 `UnsupportedClassVersionError ... class file version 69.0`，说明 JDK 不是 25（69 是 Java 25 的字节码版本）。如果报 `No ICP active`，说明上一个 JADE 实例没关干净——先完全停止上一次运行再启动。

---

## 三、学习路线图（分 9 个阶段，循序渐进）

学习顺序很重要：**每个阶段都建立在上一阶段之上**，不要跳。每学一个模块，标准流程是：

> ① 读该模块的 `readme-zh.md` → ② 打开代码看中文注释 → ③ 运行 `launch/` 或 `Main` 类 → ④ 观察 JADE GUI 弹窗 / 控制台输出 → ⑤ **动手改一改**（改参数、加一个行为、改消息内容），跑出不同结果。

| 阶段 | 主题 | 对应模块 | 你会学到 | 核心概念 |
|:---:|:---|:---|:---|:---|
| 0 | 环境验证 | `helloworldSolo` | 第一个智能体跑起来 | Agent、setup()、Runtime/Profile/Container |
| 1 | 智能体基础 | `helloworldSolo` | 智能体 + 参数 | 参数传递、命名 |
| 2 | 行为(Behaviour) | `behaviorTests` | 顺序 vs 并行行为 | Behaviour、action()/done()、OneShot/Cyclic |
| 3 | 通信基础 | `pingPong` `pingPlouf` `ticTac` | 消息收发、过滤 | ACLMessage、AID、send/receive、MessageTemplate、白页 AMS |
| 4 | 图形界面 + 广播 | `window` `radio` | 人与智能体交互、广播 | GuiAgent、GuiEvent、广播 |
| 5 | 服务管理(黄页) | `helloWorldService` `serviceDetection` | 注册/发现/订阅服务 | DFService、黄页、DFAgentDescription |
| 6 | 状态机行为 | `fsm` | FSM 组织多步行为 | FSMBehaviour、状态注册、转移 |
| 7 | 交互协议 | `protocols/*` | 拍卖、投票、请求 | AchieveRE、ContractNet、Initiator/Responder |
| 8 | 教程练习(TD) | `td/*` | 综合运用 | 协商、约束、博弈 |
| 9 | 综合案例 | `agencesVoyages` `voyagesEnVille` `issia23` `ollama` | 完整系统 + LLM | 全流程整合 |

下面按阶段展开，讲清楚每个模块"教什么、重点看哪些文件、学完要能回答什么问题"。

---

### 阶段 0-1：智能体基础（`helloworldSolo`）

**目标**：理解"一个 Agent 是什么"、"它如何被启动"。

- `AgentHello.java`：最简智能体，在控制台打印一句话。**看**：它如何继承 `Agent`、在 `setup()` 里做什么。
- `AgentHelloParametre.java`：同样打印，但消息是**启动时传进来的参数**。**看**：`Object[] getArguments()` 怎么取参数。

**学完要能回答**：
- `setup()` 是智能体的"入口/构造函数"吗？它何时被调用？
- 一个 JADE 程序里，`Runtime`、`Profile`、`Container`（容器）分别扮演什么角色？
- 智能体的名字在哪定义？同一平台内名字能重复吗？

---

### 阶段 2：行为 Behaviour（`behaviorTests`）

**目标**：理解"智能体的工作单元是行为(Behaviour)"。

- `AgentHelloSalut.java`：带简单行为的智能体。
- `AgentHelloEuropeenSequentiel.java`：2 个智能体，各自行为**顺序**执行（一个接一个）。
- `AgentHelloEuropeenParallel.java`：2 个智能体，各自行为**并行**执行（同时）。

**核心概念**：JADE 里智能体本身不直接"干活"，而是往身上 `addBehaviour()` 添加行为。每个行为有 `action()`（每次被调度时执行的动作）和 `done()`（是否结束）。多个行为可以顺序（SequentialBehaviour）或并行（ParallelBehaviour）组合。

**学完要能回答**：
- `OneShotBehaviour` 和 `CyclicBehaviour` 的区别是什么？
- `action()` 和 `done()` 各自干什么？谁在不停地调用它们？
- 怎么让多个行为按先后顺序执行？怎么让它们同时执行？

---

### 阶段 3：通信基础（`pingPong`、`pingPlouf`、`ticTac`）

**目标**：理解智能体之间**唯一的协作方式——消息**。

- `pingPong/AgentPingPong.java`：经典乒乓——ping 发球、pong 回球、循环往复。**这是必须吃透的例子**，它演示了 `send()` 和 `receive()` 的最小闭环。
- `pingPlouf/AgentPingPlouf.java`：向一个**不存在的地址**发消息，看会发生什么——收到来自**白页 AMS** 的"找不到人"通知。
- `ticTac/`：一个智能体发两种不同类型标记的消息，另一个用 `MessageTemplate`（消息模板）**过滤**，对不同类型做不同处理。

**核心概念**：
- `ACLMessage`：消息本体，有发送者、接收者（`AID`）、内容、**performative（言语行为，如 INFORM/REQUEST）**、conversationId（会话 ID，用于区分多轮对话）等。
- **白页(AMS)** vs **黄页(DF)**：AMS 是平台自带的"花名册"，记录所有智能体的名字和地址；DF 是"服务黄页"，按**服务类型**查找智能体（阶段 5 会讲）。

**学完要能回答**：
- 一条消息从发送到接收，中间经过什么？
- `MessageTemplate` 是干什么的？能按哪些字段过滤？
- conversationId / performative 分别有什么用？

---

### 阶段 4：图形界面 + 广播（`window`、`radio`）

**目标**：让智能体"看得见、说得出"，能与人交互。

- `window/`：智能体绑定一个 Swing 窗口，人通过窗口和智能体对话。**看**：`GuiAgent` 怎么与 GUI 类配合，`GuiEvent` 怎么把用户在窗口上的操作传给智能体。
- `radio/`：**广播**——一个智能体发消息到"频道"，多个监听该频道的智能体都能收到。**对比**：pingPong 是"点对点"，radio 是"一对多"。

**学完要能回答**：
- 广播和点对点通信在"指定接收者"上有何不同？
- GUI 里的操作（比如点按钮）如何变成智能体能处理的 `GuiEvent`？

---

### 阶段 5：服务管理——黄页 DF（`helloWorldService`、`serviceDetection`）

**目标**：理解智能体如何**动态地互相发现**，而不是写死对方名字。这是多智能体系统"松耦合"的关键。

- `helloWorldService/`：每个智能体向黄页（DF）注册一个服务（如"cordiality"服务），然后通过**查询 DF** 找到同服务的其他智能体，向它们发消息。**这是你之前已经跑通的例子**。
- `serviceDetection/`：更有意思——一个智能体**订阅(Subscribe)** DF，当有别的智能体**注册或注销**某服务时，它会**自动收到通知**。

**核心概念**：
- `DFService.register/deregister/search/subscribe`。
- `DFAgentDescription`：描述一个智能体提供什么服务（`ServiceDescription`：类型、名称、语言、本体等）。

**学完要能回答**：
- 为什么"查黄页找服务"比"写死对方 AID"更好？
- 注册服务和订阅服务通知，分别解决了什么问题？

---

### 阶段 6：状态机行为 FSM（`fsm`）

**目标**：当一个智能体的行为流程有多个步骤、多种分支时，用**有限状态机**来组织。

- `fsm/salutations/`：欧洲问候（握手/贴面礼）的步骤序列，用状态机串起来。
- `fsm/review/`：**论文审稿流程**——作者智能体投稿 → 期刊智能体分配 → 三个审稿人智能体评审 → 汇总结果。这是一个完整的、多智能体协作的 FSM 实例，**重点学习对象**。

**核心概念**：`FSMBehaviour`，`registerState(name, behaviour)` 注册状态，`registerTransition(from, to, event)` 定义状态转移，`registerDefaultTransition` 定义默认转移。

**学完要能回答**：
- FSM 里的"状态"在 JADE 里是什么（还是一个 Behaviour）？
- 状态之间怎么转移？"事件"从哪来？

---

### 阶段 7：交互协议（`protocols/*`）

**目标**：理解 FIPA 标准交互协议——多个智能体之间**有结构的、多轮次的**对话。这是多智能体系统里最实用也最有深度的一部分。

JADE 把协议抽象成 **Initiator**（发起方，如买家/投票发起人）和 **Responder**（响应方，如卖家/投票人）两类行为。

- `protocols/requests/`：**AchieveRE 请求协议**——一个智能体请求另一个做某事，收到同意/拒绝。**最简单的协议，从这里入手**。
- `protocols/sealedEnglishAuction/`：用 **ContractNet** 协议模拟**密封英式拍卖**（1 轮）。买家发 CFP（征求方案），卖家投 PROPOSE（报价），买家发 ACCEPT/REJECT（接受/拒绝）。
- `protocols/bordaCount/`：用 ContractNet 做 **Borda 计票投票**（选餐馆等）。
- `protocols/bordaCount5000/`：同上但 **5000 个智能体**——体会虚拟线程带来的大规模能力。
- `protocols/voteDoubleBorda/`：两轮 Borda 投票（平票时再投一次）。
- `protocols/vickrey/`：维克里拍卖（第二高价成交）。
- `protocols/voteCondorcet/`：康多塞投票法。
- `protocols/negociation/`：1 对 1 协商（待补充练习）。

**核心概念**（这是本阶段的重中之重）：
- **ContractNet** 的四个环节：`CFP`（招标）→ `PROPOSE`（投标）→ `ACCEPT/REJECT-PROPOSAL`（定标）→ `INFORM`（履约确认）。
- `ContractNetInitiator` 里的回调：`prepareCfps()`、`handlePropose()`、`handleRefuse()`、`handleAcceptProposal()`、`handleInform()`。
- `ContractNetResponder` 里的回调：`handleCfp()`、`handleAcceptProposal()`。
- 一个**计数器**（如 Borda 计票里统计票数）如何配合协议回调使用。

**学完要能回答**：
- ContractNet 的完整消息序列是怎样的？画得出来吗？
- Initiator 和 Responder 各自的 `handle*` 回调分别在什么时机被触发？
- 为什么投票/拍卖适合用 ContractNet 实现？

---

### 阶段 8：教程练习 TD（`td/*`）

**目标**：离开"示例"，做**半开放的练习**。`td/readme.md` 里是题目（多为法文，已译成 `readme-zh.md`），涉及：

- 英式公开喊价拍卖（`englishAuctionOpenCry`）
- 人机协商（`negoHumanAgents`、`negociationInteractionWindow` 等系列——买卖双方协商、加 GUI、逐步完善）
- 时间表约束求解（`timeTable`）
- 三人井字棋（TicTacMoe，融入搜索/MCTS）
- 市场（基于 Borda 改造的"社会福祉"方案）
- 稳定婚姻问题（stable marriage 算法）

**建议**：这些练习**不一定全做**，挑感兴趣的 1-2 个深入即可。它们是"从看懂到能写"的过渡。

---

### 阶段 9：综合案例 + LLM（`agencesVoyages`、`voyagesEnVille`、`issia23`、`ollama`）

**目标**：看一个完整的多智能体应用长什么样，以及如何与现代 AI 结合。

- `agencesVoyages/`：多个旅行机构智能体 + 旅客智能体，用 ContractNet 完成"询价—报价—选择"的完整业务流。
- `voyagesEnVille/`：城市出行规划——旅客发起招标，各交通方式（自行车/公交/电车/汽车）的机构从 CSV 读数据、报价，旅客按成本/时间/碳排放选择，还能处理道路预警广播。**最接近真实应用的一个例子**，且用到了 `opencsv` 和 `json`。
- `issia23/`：基于真实**循环经济**项目的小场景（咖啡机维修、配件分发、用户需求）。
- `ollama/`：**智能体接入本地 LLM**（通过 Ollama）进行对话——这是"AI Agent"方向的雏形。

---

## 四、通用学习方法（每个模块都这么做）

1. **先读 `readme-zh.md`**：知道这个模块"在干什么"。
2. **找到启动类**：通常是 `launch/LaunchXxx.java` 或 `Main.java`，看它创建了哪些智能体、传了什么参数。
3. **看一个核心智能体**：`setup()` 是入口，加了哪些行为；再看行为里的 `action()` 干了什么、`done()` 何时结束。
4. **跑起来**：JADE GUI 会弹出，里面有智能体列表；控制台有日志。
5. **改一改再跑**：这是最重要的一步。比如：
   - 把智能体数量从 9 改成 2；
   - 改消息内容；
   - 加一个 `CyclicBehaviour` 让智能体持续等待消息；
   - 把点对点改成广播；
   - 给 ContractNet 的投标价格加一个随机扰动。
6. **画消息序列图**：对协议类模块，手动画出"谁给谁发了什么消息、顺序如何"，能极大加深理解。

---

## 五、学完之后：工程实践与发展方向

学完这套教程，你已经掌握了多智能体编程的核心范式（Agent / Behaviour / 消息 / 黄页 / 协议）。接下来可以往以下几个方向发展：

### 方向 1：深化理论与框架机制（打牢地基）
- 精读 **FIPA 规范**（ACL 消息、交互协议），理解 performative 的完整语义。
- 深入研究 **JADE 的分布式能力**：多容器（跨主机）、Agent 迁移（Mobility）、安全（加密通信）。
- 了解 **BDI 模型**（信念-愿望-意图）——这是 JADE 之外更高级的智能体心智模型，代表框架是 **Jason**（AgentSpeak 语言）。

### 方向 2：多智能体仿真（Simulation，最对口 JADE）
JADE 特别适合**离散事件仿真**，工业界用它做：
- **智能交通**（车辆/路口/信号灯协同）——正好对应 `voyagesEnVille` 的方向。
- **供应链与物流**（订单、库存、运输、拍卖采购）——对应 `agencesVoyages` / ContractNet。
- **电力市场 / 能源调度**（发电方、用电方、电网交易）。
- **社会仿真**（人群疏散、舆论演化、市场机制设计）。
- 可以结合 **Repast / GAMA / NetLogo** 等仿真平台理解更广的生态。

### 方向 3：AI / LLM 智能体（当前最热）
- 从 `ollama` 模块出发，把 **LLM 作为智能体的"大脑"**：让智能体用自然语言做决策、调用工具（Function Calling）。
- 了解现代 Agent 框架的脉络：`LangGraph`、`AutoGen`、`CrewAI`、`Semantic Kernel` 等。你会发现自己已经理解了它们的底层概念（消息、角色、工具调用、多轮对话、状态机）。

### 方向 4：转向现代 / 生产级技术栈
JADE 偏学术、较老，工程上如果想做真正可部署的多智能体系统，可以了解：
- **Akka / Pekko**（JVM 上的 Actor 模型，与 Agent 思想同源，但更现代、并发更强）；
- 分布式消息中间件（Kafka、RabbitMQ）如何支撑大规模异步协作；
- **Kubernetes** 上的微服务如何演化为"微服务 + Agent"。

### 方向 5：参与社区与竞赛
- 关注 **JADE 官方/衍生项目**（JadeUPHF、Jason、SPADE、Mesa/NetLogo 社区）。
- 多智能体方向有 **竞赛**（如 RoboCup 仿真赛、TAC 贸易智能体竞赛），是极好的实战项目。

### 一个务实的建议
对初学者，**先把阶段 0-7 完整过一遍**（这是 JADE 的核心），阶段 8-9 挑感兴趣的做。学完后若想继续，**"方向 3（LLM Agent）+ 方向 2（一个具体的仿真场景）"组合**最能体现价值：做一个"用 JADE 协作 + 用 LLM 决策"的小 demo，既呼应了本仓库 `ollama` 模块，又贴近当前就业市场对 Agent 工程师的需求。

---

## 附：快速定位表（模块 → 学什么 → 启动类）

| 模块 | 一句话主题 | 启动类 |
|:---|:---|:---|
| `helloworldSolo` | 最简智能体 | `AgentHello` / `AgentHelloParametre`（含 main） |
| `behaviorTests` | 顺序/并行行为 | 各 `AgentHello*` 类（含 main） |
| `pingPong` | 点对点消息往返 | `AgentPingPong`（含 main） |
| `pingPlouf` | 发消息到无效地址 | `AgentPingPlouf` |
| `ticTac` | 消息过滤 | `LaunchAgents` |
| `window` | 智能体+窗口 | `launch/LaunchAgents` |
| `radio` | 广播 | `launch/LaunchAgents` |
| `helloWorldService` | 黄页服务发现 | `launch/LaunchAgents` |
| `serviceDetection` | 订阅服务变化 | `launch/LaunchAgents` |
| `fsm/salutations` | FSM 问候 | `AgentHelloEuropeenFSM` |
| `fsm/review` | FSM 审稿流程 | `launch/LaunchAgents` |
| `protocols/requests` | 请求协议 | `launch/LaunchAgents` |
| `protocols/sealedEnglishAuction` | 密封英式拍卖 | `launch/LaunchAgents` |
| `protocols/bordaCount` | Borda 投票 | `launch/LaunchAgents` |
| `protocols/bordaCount5000` | 5000 智能体投票 | `launch/LaunchAgents` |
| `protocols/voteDoubleBorda` | 两轮投票 | `launch/LaunchAgents` |
| `protocols/vickrey` | 维克里拍卖 | `launch/LaunchAgents` |
| `protocols/voteCondorcet` | 康多塞投票 | `launch/LaunchAgents` |
| `protocols/negociation` | 1 对 1 讨价还价 | `Negociateur`（含 main） |
| `td/englishAuctionOpenCry` | 公开喊价拍卖（作业） | 见各作业目录 |
| `td/negociation*` 系列 | 协商作业（多版本） | 各目录下的 `Main` |
| `td/timeTable` | 约束求解（作业） | 见目录 |
| `agencesVoyages` | 旅行机构协商 | `launch/LaunchSimu` |
| `voyagesEnVille` | 城市出行规划 | `launch/LaunchSimu` |
| `issia23` | 循环经济场景 | `launch/LaunchAgents` |
| `ollama` | LLM 智能体 | `launch/LaunchAgents` |

> 注：本指导与各模块目录下的 `readme-zh.md`、代码中文注释配套使用。遇到看不懂的类或方法，优先看代码里的中文注释，再回来看这里的"核心概念"。

### 已加的中文资料一览

仓库里目前所有中文辅助资料：

- **本文件** `学习指导.md`：全局路线
- **35 个 `readme-zh.md`**：每个模块的中文 readme（与原文 `readme.md` 并列）
- **Java 代码里的中文注释**：关键类和核心算法已加 `【中文标签】` 块注释，重点覆盖了：
  - `helloworldSolo`、`helloWorldService`、`pingPong`、`pingPlouf`：消息机制入门
  - `behaviorTests`、`serviceDetection`：行为与服务发现
  - `fsm`（`salutations` + `review`）：状态机与投稿流程
  - `protocols/requests`（AchieveRE 协议）、`protocols/bordaCount`（ContractNet 投票）、
    `protocols/negociation`（1v1 协商）、`protocols/sealedEnglishAuction` + `vickrey`（两种拍卖）
- **未加详细注释**：`td/*` 作业题（学生要自己实现）、`agencesVoyages` + `voyagesEnVille` 的复杂业务类、
  `protocols/bordaCount5000`（是 bordaCount 的"性能版"，注释可类比）、`issia23` 和 `ollama`（进阶模块）
